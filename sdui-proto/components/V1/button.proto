syntax = "proto3";

package components;


message SduiButton {
    string label = 1;
    bool enabled = 2;
    Action action = 3;
}

message Action {
    ActionType action_type = 1;
    string action_value = 2;
}

enum ActionType {
    ACTION_NAVIGATION = 0;
    ACTION_TYPE_API_CALL = 1;
}